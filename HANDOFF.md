# Reading Coach / Bookie · 项目交接手册

> **用途**：这是后续 Work thread 的项目事实来源。开始任何新功能、修 bug 或部署前，先读本文件，再检查仓库当前状态；不要用聊天记忆覆盖这里记录的已确认决策。
>
> **项目目录**：`/Users/zhouziyue/Desktop/我的运营文档/pe-reading-classroom`  
> **公开测试网址**：[Bookie · PE Reading Studio](https://bookie-pe-reading-studio.ivy-zhou0401.chatgpt.site)  
> **GitHub 仓库**：[ivyzhou891213/bookie-pe-reading-studio](https://github.com/ivyzhou891213/bookie-pe-reading-studio)  
> **最后本地提交**：`a46fde8`（`Retry AI planning and retain complete fallback schedule`）

---

## 1. 产品目标与整体架构

### 1.1 产品定位

Reading Coach（产品界面名为 **Bookie · PE Reading Studio / PE 阅读教室**）是一个首先服务于用户本人的、面向 PE / 投行能力建设的长期学习工作台。它把以下动作连成闭环：

`上传合法使用的 PDF → 阅读、划线与提问 → 记录真实阅读范围 → AI 生成短回顾 → 自动归档 → 按书籍、主题、公司或行业复习`

它不是普通 PDF 阅读器，也不是泛化聊天机器人。产品的核心价值是：让阅读计划、阅读过程、提问、笔记、知识卡、面试资料与投资素材留痕、可回看、可检索。

### 1.2 当前架构

| 层级 | 当前实现 | 事实与边界 |
| --- | --- | --- |
| 前端 | React 19 + TypeScript + Vinext，主要实现集中在 `app/page.tsx` | 单页多视图应用；没有 Next 路由分页面。 |
| PDF 阅读 | PDF.js + 浏览器 Canvas/TextLayer | 可选择文字的 PDF 直接读取文字层；扫描件使用 OCR。 |
| OCR | Tesseract.js，浏览器本地运行 | 整书 OCR 和单页 OCR 的文字结果保存本机，不消耗 AI token。 |
| 本地文件 | IndexedDB：数据库名 `bookie-private-library`，stores 为 `pdfs`、`ocrPages` | 已上传 PDF 与 OCR 页文本存在当前浏览器/设备；不是云端同步。 |
| 用户记录 | `localStorage` 键 `pe-classroom-store` | 保存计划、问答、笔记、进度、知识卡、归档等；清理浏览器数据会丢失。 |
| AI 设置与用量 | API Key 在 `localStorage`；日用 token 估算也在本地 | Key 不能写入源码、GitHub、部署变量或 HANDOFF。 |
| AI 服务 | `/api/mentor`、`/api/knowledge`、`/api/planner`、`/api/formulas`、`/api/mock`、`/api/import` | 服务端把用户浏览器提交的 Key 直接转发给所选模型；当前默认 DeepSeek。 |
| 部署 | Sites/Cloudflare 的公开测试站；`.openai/hosting.json` 含现有 project id 与 D1 逻辑绑定 | 当前尚未使用 D1；`db/schema.ts` 为空。 |

### 1.3 隐私、版权与上线边界

- 当前版本是 **local-first 原型**。公开网址运行网页代码，但用户的 PDF、OCR、笔记、阅读记录与 Key 不应随网站一起上传或共享。
- 公开版本不应自带受版权保护的书籍 PDF。使用者只能上传自己拥有合法使用权的文件；不要把盗版书籍、个人资料或 Key 提交到 GitHub / 部署产物。
- 面向多人正式上线、跨设备同步、长期云存储或付费前，必须重新设计账号、授权、对象存储、数据库、限额与版权政策；这不是当前版本已完成的能力。

---

## 2. 已完成的功能与当前网页状态

### 2.1 页面 / 视图

| 页面 | 当前状态 | 已完成动作 |
| --- | --- | --- |
| 我的书桌（首页） | 已实现 | 展示当前计划、总体进度、书架、创建/编辑计划入口；每日计划卡支持状态猫。 |
| 我的书架 | 已实现（首页区块） | 上传 PDF、显示封面和进度；标示扫描件/低清 PDF；可触发整书 OCR 或重建目录。 |
| 阅读 | 已实现 | PDF 显示、页码导航、章节选择、文字选区、语音输入、导师问答、开始/结束本次阅读、生成回顾。 |
| 读书笔记 | 已实现 | 按书籍、日期和关键词检索问答与会话回顾；当天回顾自动归档。 |
| 知识库 | 已实现 | 保存知识问答 / 公式卡，支持从历史笔记进行知识检索。 |
| 面试训练 | 已实现 | 粘贴或导入面经、AI 归档、技术题生成、回答评估、历史回看。 |
| 投资雷达 | 已实现 | 粘贴文章或交易材料、AI 分析并按公司/行业/主题/标签自动归档。 |
| 设置 | 已实现 | DeepSeek / OpenAI Key、模型/推理档位、token 用量估算、本地数据导出与清理说明。 |

### 2.2 阅读闭环

1. 用户上传 PDF；带文字层的 PDF 可直接识别目录，扫描件提示先进行整书 OCR。
2. 阅读器记录本次开始页、访问页、结束页、阅读时长和期间提出的问题。
3. 用户划选文字后可提问；翻译请求只给简洁中文，其他常规问答应中英双语且简短。
4. “生成本次回顾”基于**实际阅读行为**和当次问答生成，自动保存至读书笔记；不能假设“今天固定读了十页/两章”。
5. 读书笔记、知识库、面试训练、投资雷达都需要是可检索档案，而不是一次性聊天输出。

### 2.3 OCR 与目录

- 上传书籍会生成第一页封面图；若封面解析失败显示占位图，不应假装识别成功。
- 扫描件会提示“需要整书 OCR”；OCR 进度和预计耗时应可见。整书完成后，阅读器应复用保存的 OCR 文本，不能要求逐页再次 OCR。
- `rebuildBookContents()` 用原生文字层重建章节；`runFullBookOcr`（在 `app/page.tsx` 同一组件中）对全文做 OCR 并抽取章节。
- 目录少于 4 章时，计划生成被阻止，提示先重建目录或进行整书 OCR。

### 2.4 计划与进度

- 一个计划最多可选 3 本书；同一时间段最多可并行 3 个计划。
- 计划预览展示逐日卡片，而非只有按周的大段文字。卡片包含日期、当天时间、书名与章节范围、简短中文重点。
- 日卡有 Bookie 三态：完成为开心、过去未完成为不开心、未来/当天为加油。状态来自 `completedChapters` 和本地日期。
- 日卡可点击并尝试打开当天第一个章节；此处仍有重要导航 bug，见第 6 节。
- 最近版本把 AI 置于逐日排程位置：它收到完整目录、章节页数、固定日期及每日可用时长；客户端检查全书是否恰好覆盖、无重复、顺序正确。格式不合格时会自动再试一次；仍失败时展示保证完整覆盖的保底安排。

### 2.5 AI 功能与模型分工

| 工作 | 当前默认模型 | 行为约束 |
| --- | --- | --- |
| 阅读陪读、翻译、解释、阅读回顾、知识检索、公式整理、资料归档、学习计划 | DeepSeek Flash | 轻量、短输出、控制上下文与 token；翻译仅中文，其余常规问答中英双语。 |
| 高难 PE / 投行技术模拟 | DeepSeek Pro | 仅面试模拟等高难推理使用；未配置 OpenAI 不影响其它功能。 |
| OpenAI | 未来可选 | 目前不是日常必需；不应在未填 Key 时自动调用。 |

当前 `aiFor()` 的真实路由：`mock` 使用 `deepseek-v4-pro`，其他任务为 `deepseek-v4-flash`。设置页允许用户调整 Flash / Pro 的思考状态与推理档位；在 UI 中应保持可见、可解释，不能隐蔽地升级到贵模型。

---

## 3. Valuation 与 Investment Banking（M&A）两本书

> 用户有时口头称第二本为 “M&A 书”；当前代码中的正式默认书名是 **Investment Banking**，内容覆盖 Comps、Precedents、DCF、LBO、Sell-side M&A、Buy-side M&A、IPO。

### 3.1 默认书目（仅用于本地开发演示；公开站不得携带 PDF 文件）

| 书 | 内部 id | 页数 | 章节 | 目录事实 |
| --- | --- | ---: | ---: | --- |
| McKinsey Valuation | `valuation` | 1,342 | 43 | `CHAPTERS.valuation` 中有 Ch.1–43 及已知 PDF 正文起始页。 |
| Investment Banking | `investment-banking` | 514 | 9 | `CHAPTERS['investment-banking']` 中有 Ch.1–9 及已知正文起始页。 |

- Valuation 章节包含价值创造、ROIC、增长、估值、DCF、资本成本、M&A、LBO、VC、高增长和特殊行业等；Ch.25 是 **CFROI**。
- Investment Banking 的 9 章依次为 Comps、Precedents、DCF、LBO、LBO Analysis、Sell-Side M&A、Buy-Side M&A、IPO、IPO Process & Execution。
- 计划应使用读者实际上传书籍的目录；默认 `CHAPTERS` 是已知示例映射，不能把默认书名或目录误用于另一版本 PDF。

### 3.2 阅读计划的硬要求

1. 当用户选择两本书并写“4 周内完成”，计划必须覆盖两本书的**全部**章节；不能只因平均速度估算而排到 Valuation Ch.28。
2. AI 必须根据工作日和周末不同的可用时长、每章页数/相对长度与章节顺序决定逐日分配。
3. 所有章节必须恰好一次、同一本书顺序递增、在截止日期前完成；不能插入用户明确拒绝的复盘日、休息日或空任务。
4. 计划重点应是与当日真实章节对应的一句精炼中文，不要反复出现同一段 “深度阅读 Why Value Value?”，不要用 `MY · Ch.1` 这类无书名代码。
5. AI 无法给出可验证的排程时，系统使用完整覆盖的保底安排并明确标识；不能展示一份看似 AI 生成却漏章的计划。

---

## 4. 已确认、不得擅自修改的产品与设计决策

以下是用户已明确确认的事实。后续开发不能自行取消、改弱或改成另一种产品逻辑；若确需改变，必须先提出理由并征得确认。

### 数据、成本与部署

- 第一阶段维持 local-first：个人 PDF、OCR、学习记录和 API Key 在用户自己的浏览器 / 电脑中。
- 用户自备 API Key、承担自己调用的模型费用；产品必须显示 token 用量或成本提示，并尽量节省上下文与输出。
- OCR 必须本地运行、可复用，不能每次翻页都收费或重新识别。
- 公开测试站仅提供网页代码与本地上传能力，不分发用户书籍；版权合规优先。
- GitHub 用于保存代码和版本历史，**不**等于保存个人 PDF、OCR 或浏览器学习数据。

### AI 体验

- Prompt 内置于按钮 / 流程，用户不应每次编写长 prompt。
- Prompt 必须动态引用实际行为和实际材料；不能写死“今天读了十页/两章”。
- 翻译：用户明确请求翻译或看不懂时，仅给简洁中文翻译，必要时一行关键词；不加 PE / 面试延伸。
- 其它普通阅读问答：紧凑中英双语；最多约 3 个要点，不能课程式长文。
- 阅读回顾：基于本次实际页数、问题与选区，最多 5 个要点、简洁且格式清晰；不要把整章重新复述。
- AI 输出必须保存 / 归档，不能因问下一个问题就消失。

### 信息组织与视觉

- 导航保留：我的书桌、阅读、读书笔记、知识库、面试训练、投资雷达、设置。
- 面试训练与投资雷达：分析完成后按公司、行业、主题、标签自动归档；用户应能筛选、进入文件夹复盘历史记录。
- 读书笔记：按书籍、日期、关键词和阅读场次归档；知识库：用于公式、概念和较复杂的应用问题，而非杂乱聊天。
- 学习计划以**逐日卡片 / 日历式阅读安排**为主，不退回只有周卡的大段章节清单。
- 保留开心 / 加油 / 不开心 Bookie 状态猫及日卡点击入口。
- 中文界面中的计划重点使用中文；不要强制用户再看一遍冗长英文摘要。
- 现有视觉风格为苹果式白底、雾蓝/鼠尾草/淡紫辅助色、石墨文字、圆角卡片；不要无理由整体重做。

---

## 5. 当前阅读时间规则

### 5.1 规则来源

时间由用户在计划描述框中自然语言输入，`extractPlanFromBrief()` 解析：

- 周数：例如 `4 周`；
- 工作日时间段：例如 `工作日晚上 10 点到 12 点`；
- 工作日每日时长：例如 `每天 1 小时`；
- 周末每日时长：例如 `周末每天 9 小时`。

### 5.2 当前用户示例

用户当前常用目标是：**4 周内系统完成 McKinsey Valuation 与 Investment Banking，用于跳槽大型 PE 平台；工作日晚上 22:00–24:00，周末每天 9 小时。**

因此典型周容量为：

- 工作日：5 × 2 小时 = 10 小时；
- 周末：2 × 9 小时 = 18 小时；
- 合计：约 28 小时/周、112 小时/4 周。

这只是用户给出的可用时间，不是“每章固定 3 小时”的算法。AI 排程必须把长章节更多安排到周末或更长的可用时段，并保证全书覆盖。

### 5.3 实现现状

- `hoursFromText()` / `scheduleHours()` 解析时长；`dailyRowsForSchedule()` 是完整覆盖的保底分配器，并以可用小时权重使周末承担更多章节。
- `/api/planner` 让 AI 输出 `dailySchedule`（week、day、连续章节范围、中文 outcome）。
- `validateAiDailySchedule()` 检查章节集合、重复、顺序和日期行；首轮无效会自动请求一次 repair，仍无效则使用保底日程。
- **注意**：计划不能声称 AI 已完成章节重点，除非 `aiPlanApplied === true` 且验证通过。

---

## 6. 当前已知 bug / 风险

### P0：章节跳转会打开目录页而不是正文页（未修复）

**现象**：点击日卡，例如 `McKinsey Valuation · Ch.25–Ch.28`，阅读器可能跳到 PDF 第 8 页附近的目录，看到 “CHAPTER 25” 的目录条目，而不是 Ch.25 正文。

**原因（已定位）**：

- `chapterFromLine()` 从匹配行末尾提取数字，优先把目录上的 printed page / 列表页码当作 `chapter.page`；
- `rebuildBookContents()` 和 OCR 目录扫描会将目录页写入上传书籍的 `chapters`；
- `openPlanUnit()` 直接用该 `chapter.page` 打开阅读器，并未验证正文标题。

**验收标准**：点击任意日卡的 Ch.N，打开的是该书正文中 Ch.N 的真实起始页（不是目录页）。需要在跳转前扫描 / 缓存真实页，或在目录提取时严格区分 TOC 和正文标题；修复后应刷新已保存上传书籍的错误 `chapters` 映射。

### P1：AI 计划成功率与成本仍需真实验证

- 最近版本已经缩短 planner prompt、去掉正文摘录、自动 repair 一次并提供保底日程；但尚未用用户实际 DeepSeek Key 完整验证一次 “两书、4 周、28 天”的成功输出。
- 需要确认 Flash 是否稳定输出每一天一行合法 JSON；若不稳定，应改善 schema / 解析或让用户可接受保底安排，而不是增加无限重试和 token 消耗。
- UI 当前会显示保底安排提示；需确认文案不会让用户误以为计划已由 AI 成功生成。

### P1：已保存的旧计划可能仍是旧的部分覆盖日程

- 老计划对象可能持有旧 `schedule`（当时只排到部分章节）。新建或重新生成计划会使用完整覆盖逻辑。
- 后续应考虑一次性迁移 / 检测：若旧计划的章节集合不等于当前 `bookIds` 全目录，提醒用户重新生成，或安全地重建它。

### P1：目录识别准确率依赖 PDF 版本

- OCR / 文字层可正确读出 43 / 9 章不等于起始页准确。
- 上传书籍标题、封面和章节数有过错误报告；对于每本新 PDF，须显示真实识别状态，并提供“重建目录”或“整书 OCR”而非假设成功。

### P2：公共站与本地上传路径需要统一审计

- 客户端主流程是 local-first IndexedDB；但 `app/api/books/route.ts` 仍有一个服务器写入 `public/uploads` 的上传 API（180 MB 上限）。
- 后续若公开多人使用，应明确只保留何种上传路径，并确保公开部署不把用户文件写入共享静态目录。当前不要把它当作云端存储方案。

### P2：历史响应式布局需回归测试

- 曾出现右侧计划面板挤压变形、首页重复“创建学习计划”入口等问题，相关 UI 已多次修改。
- 后续改动首页、计划弹窗、宽屏/窄屏布局时，必须检查桌面和窄窗口；不能因为修一个页面又出现第二个创建入口或横向溢出。

---

## 7. 尚未完成的任务（按优先级）

1. **修复章节正文跳转**（P0）：见第 6 节，先解决再做新的大功能。
2. **用真实数据验证 AI 全覆盖计划**（P1）：用两书、4 周、工作日 22:00–24:00 / 周末 9 小时，确认 Valuation Ch.43 与 Investment Banking Ch.9 都在截止前排入日卡；检查无复盘/休息日、无重复章节、日卡重点真实对应章节。
3. **旧计划迁移或显著提示**（P1）：防止旧 schedule 延续部分覆盖的历史 bug。
4. **OCR 目录可靠性治理**（P1）：正文页定位、目录页排除、重建后的状态提示、封面读取失败的明确反馈。
5. **真实 token 用量与成本优化复查**（P1）：记录每次 planner/mentor/summary 的实际 usage；避免把整本书或大量 OCR 文本传给模型；评估 auto-repair 是否可接受。
6. **端到端回归检查**（P2）：上传 → 识别 / OCR → 建计划 → 点日卡 → 阅读与提问 → 结束阅读 → 回顾入读书笔记 → 知识检索 / 归档。
7. **多人 / 云端版本规划（暂不实施）**：只有用户明确决定做多人正式产品时，再设计登录、数据库、对象存储、配额、用户自带 Key、付费和版权策略。

---

## 8. 代码结构、关键文件与运行方式

### 8.1 文件地图

| 文件 | 作用 |
| --- | --- |
| `app/page.tsx` | 主应用：类型、书目和章节、localStorage / IndexedDB、所有页面、阅读器、OCR、计划、AI 调用客户端逻辑。当前最大的关键文件。 |
| `app/globals.css` | 全局主题、阅读器响应式网格、日卡 Bookie 动效。 |
| `app/layout.tsx` | 中文 metadata 与全局字体。 |
| `app/api/mentor/route.ts` | 划线提问、翻译、解释；有严格的短输出 / 翻译仅中文规则。 |
| `app/api/knowledge/route.ts` | 知识库检索与当天阅读回顾。 |
| `app/api/planner/route.ts` | AI 完整逐日排程 JSON；当前最大输出 5,000 tokens。 |
| `app/api/formulas/route.ts` | 从 PDF 页段抽取公式并建立双语面试表达。 |
| `app/api/mock/route.ts` | DeepSeek Pro 技术题生成与面试回答评估。 |
| `app/api/import/route.ts` | 面经 / 投资资料 AI 分析及自动分类。 |
| `app/api/books/route.ts` | 服务器上传旧路径，见已知风险，不要在未审计前扩展。 |
| `lib/formulas.ts` | 默认公式卡。 |
| `db/schema.ts` | 空占位；尚没有实际数据库 schema。 |
| `db/index.ts` | D1 Drizzle helper；当前无业务调用。 |
| `.openai/hosting.json` | 现有 Sites 项目配置；不得新建第二个站点 project。 |
| `README.md`、`使用说明.md` | 对外说明；后续功能大改后应同步更新。 |
| `产品开发与维护手册.md` | 产品开发方法论；本交接文档优先记录本项目事实。 |

### 8.2 关键函数定位（`app/page.tsx`）

| 函数 / 区域 | 作用 |
| --- | --- |
| `BOOKS`、`CHAPTERS` | 默认两本书及章节→PDF 正文起始页的映射。 |
| `bookDatabase()`、`saveLocalPdf()`、`loadLocalPdf()`、`saveOcrPage()` | IndexedDB 本地文件/OCR 存取。 |
| `aiFor()` | AI 模型路由；不要绕开它在各页面硬编码模型。 |
| `buildSchedule()` | 保底完整覆盖的周计划；不再按固定“3 小时一章”截断整本书。 |
| `dailyRowsForSchedule()` | 按工作日/周末小时权重的保底逐日分配。 |
| `validateAiDailySchedule()` | 验证 AI 日程完整性和章节顺序。 |
| `extractPlanFromBrief()` | 解析用户自然语言时间、请求 AI、自动 repair、生成 `StudyPlan`。 |
| `rebuildBookContents()` / 全书 OCR 逻辑 | 重建章节目录；当前正文页定位 bug 的相关区域。 |
| `openPlanUnit()` | 日卡→阅读器跳转；当前 P0 bug 的直接入口。 |
| `DailyStudyCalendar()` | 每日卡、Bookie 三态、点击日卡。 |
| `PlannerModal()` | 创建/编辑计划交互。 |
| `PdfPage` / `runOcr()` | 阅读器页面渲染、文字层与单页 OCR。 |

### 8.3 本地运行

前置条件：Node `>=22.13.0`，项目使用 pnpm lockfile。

```bash
cd "/Users/zhouziyue/Desktop/我的运营文档/pe-reading-classroom"
pnpm install
pnpm dev
```

- `启动PE阅读教室.command` 会启动 `vinext dev` 并打开 `http://localhost:3000/`。
- 生产检查：`CI=true ./node_modules/.bin/vinext build` 或 `pnpm build`。
- 不把用户 API Key、PDF、OCR 缓存、浏览器数据、`.env` 加入 Git。

### 8.4 Git 与部署状态（必须先检查）

- 当前本地分支：`main`。
- GitHub remote：`origin` 指向 `https://github.com/ivyzhou891213/bookie-pe-reading-studio.git`。
- **截至本手册写入时，本地 `main` 比 `origin/main` 领先 11 个提交。** 已部署的代码不等于 GitHub 远端已同步；下一位开发者应先确认是否需要把这些提交推到用户的 GitHub 仓库，不能假设已经完成。
- 公开测试站最近部署版本来自本地提交 `a46fde8`，测试网址见文首。
- 发布流程：修改本地 → 生产 build 成功 → commit → 推送部署源 → 打包 `dist`（不含个人 PDF）→ 保存版本 → 在用户明确允许后发布 → 检查部署成功。GitHub 与 Sites 部署源是两条不同的远端，不能混淆。

---

## 9. 后续 Work thread 的工作规则

1. 开始时先读 `HANDOFF.md`、检查 `git status -sb`、确认当前用户要解决的单一问题。
2. 对 bug 先写清：页面、期望、实际、稳定复现步骤、涉及的本地数据 / API，再改代码。
3. 不要因一个视觉问题重构整个 `app/page.tsx` 或改变已确认信息架构；先做小范围、可验证修复。
4. 每个 AI 按钮都要说明动态输入、模型、最长输出、是否保存、token 成本；不能把整本 PDF 无限制发给模型。
5. 不要部署现有公开站，除非用户在该次请求中明确同意发布。
6. 每个已完成变更：运行 build，提交清晰 commit；若涉及公开测试网址，部署同一提交并报告 URL。
7. 文档、README 和用户说明若与实际能力发生实质变化，应同步更新；此文件也应随重大决定更新。

---

## 10. 下一位 Work thread 可直接使用的开场提示

```text
继续开发 Reading Coach / Bookie 项目。
项目目录：/Users/zhouziyue/Desktop/我的运营文档/pe-reading-classroom
请先完整阅读 HANDOFF.md，并检查 git status -sb；不要假设 GitHub 是最新版本。

本次任务：修复“点击每日计划卡后打开目录页而非章节正文”的 P0 bug。
验收：点击 Valuation Ch.25–Ch.28 的日卡时，阅读器打开 Ch.25 正文起始页，而不是 PDF 目录页；对上传书籍同样有效；已有错误章节页映射能被安全刷新。

不要改变已经确认的 local-first、用户自带 API Key、完整覆盖计划、工作日/周末差异排程、中文精炼重点、自动归档与 Bookie 日卡状态等规则。完成后 build、提交；发布前先征得我的明确同意。
```

---

## 11. 2026-09-10 计划生成修复记录

- 每日章节范围仍由本地可验证分配器决定；DeepSeek 只生成并润色每日中文学习重点。不允许 AI 修改已验证的章节覆盖。
- DeepSeek 返回缺行或格式异常时，仍要显示完整的时间、章节和基于实际章节标题的重点；不能再显示空白的“AI 重点未生成”卡片。
- 计划预览要明确显示 DeepSeek 的成功状态或本地保底状态；日卡时段在独立行显示，不得被七列布局按缩。
- Sites 发布包必须不包含 `dist/client/books/*.pdf`；这些是可以在本地构建中残留的开发用文件，不得发布为公开站点资产。
