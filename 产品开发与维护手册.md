# Bookie 产品开发与维护手册

这份手册的目的不是让你把所有想法一次想完，而是让每一轮修改都有边界、有验证、有记录。第一次做产品时，反复打磨是正常的；下一次要优化的是“每次打磨的方式”。

## 一、先判断：这是产品发现，还是代码返工？

这次 Bookie 的前半段属于产品发现：你是在真实使用中才逐渐发现“按周计划应可点击”“OCR 不能逐页执行”“AI 回顾必须来自实际阅读区间”“归档必须可检索”。这些不是无效返工，而是需求从抽象变具体。

真正可避免的返工，通常来自三个缺口：没有先写清核心使用路径、没有把数据如何保存想清楚、没有为每个 AI 按钮定义输入和输出。

## 二、下一次的标准开局：一页产品蓝图

在让 AI 写代码前，先把下面内容写成一个短文档。答案可以修改，但第一版先足够清楚。

| 要回答的问题 | 英语学习网站示例 |
| --- | --- |
| 第一位用户是谁？ | 工作日每天 30 分钟、想提高商务英语表达的中文母语者 |
| 一次完整使用流程？ | 选一篇材料 → 阅读/跟读 → 标记不懂处 → 完成练习 → 查看回顾 |
| 最小可用版本只做什么？ | 材料库、单次学习、错题/笔记留档、当日回顾 |
| 暂时不做什么？ | 社区、排行榜、多设备同步、付费体系 |
| 数据放在哪里？ | 第一版：浏览器本地；多人版：账号 + 云端数据库/对象存储 |
| AI 做什么？ | 翻译、解释、短回顾、口语反馈；每项都设模型与输出上限 |

再给每个页面写一句验收标准。例如：

> 用户结束一次学习后，能看到本次材料、完成内容、最多三条重点、自己的问题和回答；刷新后这些记录仍在。

没有验收标准的想法先放进“以后再说”，不要直接进入开发。

## 三、按“完整小闭环”开发，而不是按页面堆功能

推荐的顺序：

1. **数据进入**：上传/创建一份学习材料。
2. **学习动作**：阅读、标记、回答或跟读。
3. **结果保存**：把完成状态、笔记、问题保存下来。
4. **回顾检索**：第二天仍能按材料、主题和日期找到它。
5. **AI 增强**：先加一个真正有用的 AI 按钮，再扩大范围。

每完成一个闭环，就真实使用 10–20 分钟，并只记录三类反馈：阻断使用的 bug、明显不顺手的体验、以后再做的新功能。先修第一类，再处理第二类；第三类集中排期。

## 四、AI 功能必须先写“按钮合同”

每个 AI 按钮应有下列定义；这是控制成本、避免回答跑题的关键。

| 按钮 | 动态输入 | 输出 | 默认模型 | 上限 | 保存位置 |
| --- | --- | --- | --- | --- | --- |
| 翻译选中内容 | 选中的文字和用户语言 | 只给翻译，必要时一句说明 | 轻量模型 | 120 tokens | 当前问答/笔记 |
| 解释概念 | 选中内容 + 用户问题 | 3–5 条简洁双语解释 | 轻量模型 | 300 tokens | 知识卡 |
| 本次学习回顾 | 实际开始/结束页或题目、标记、问题 | 3–5 条要点 + 待复习项 | 轻量模型 | 420 tokens | 当日笔记和归档 |
| 高难模拟 | 用户回答 + 题目 + 少量相关记录 | 评分、追问、改进建议 | 强模型 | 700 tokens | 训练记录 |

原则：输入必须来自真实行为；输出必须短且有格式；结果必须保存；页面重新打开不能自动再次调用模型。

## 五、怎么省 token、减少反复对话

- 每轮集中反馈：把同一页面的问题合并成一份列表，按“必须修 / 应该修 / 以后再说”排序。
- 先要方案，后要实施：复杂改动先让我给出受影响页面、数据和验收方式；确认后再写代码。
- 不用 AI 重复读整本书：只把当前选区、当前会话、或已经保存的摘要送入模型。
- 所有 AI 输出设长度上限：翻译、解释、回顾、模拟各自不同；不要使用一个“大而全”的 prompt。
- 使用项目文档做外部记忆：新对话先给我产品蓝图、当前目标、相关文件、验收标准，而不是复制整个历史聊天。

## 六、网站以后怎么维护

日常维护的唯一流程：

```text
发现问题 → 写一个清楚的 bug/需求单 → 本地修改 → 本地验证 → Git commit
→ 推送 GitHub → 部署同一个 commit → 打开正式网址复核
```

不要一边直接在 GitHub 网页改代码、一边在本地改同一文件；本地仓库应是唯一工作副本。

报告问题时，按这个模板发给我即可：

```text
页面：
期望发生什么：
实际发生什么：
如何稳定复现：
截图/录屏：
是否涉及已上传数据或 API：
```

### 什么时候新开一个对话？

- **继续当前对话**：同一个 bug 的补充、刚完成改动的验证。
- **新开对话**：新的大功能、视觉重做、数据库/登录/部署、或想法已换成另一条产品线。

新对话开头只需提供：项目路径/仓库链接、这次要做的事、不可改变的规则、验收标准、相关截图。这样既不会丢规则，也不会让聊天记录无限变长。

## 七、GitHub、网址与数据分别负责什么

- **GitHub**：保存代码的历史、让你回退版本、让别人查看或协作；不保存用户的浏览器资料。
- **公开测试网址**：运行网站给他人打开；目前是临时托管地址。
- **浏览器本地存储**：目前保存 PDF、OCR、API Key、阅读记录和笔记；换设备或清除浏览器数据不会自动保留。
- **未来的数据库/对象存储**：只有在做登录、跨设备同步或多人正式使用时才需要加入。

## 八、这次可以迁移到英语学习网站的部分

可以直接复用：页面导航、上传与本地文件保存、会话开始/结束记录、AI 问答与摘要、双语输出、归档筛选、token 估算、设置页、GitHub/部署流程。

不应直接复制：PE 专业内容、估值公式、面试题库、投资雷达分类，以及目前为 PDF 阅读设计的具体章节规则。英语学习需要重新定义材料单位（文章、音频、单词、口语任务）与进度单位（段落、句子、题目、录音）。

## 九、给下一次开发的开场提示词

```text
我要做一个 [产品名称]，第一位用户是 [用户]。
核心学习闭环是：[进入材料] → [学习动作] → [保存结果] → [之后回顾]。
第一版只做：[最多 3–5 项]；明确不做：[内容]。
数据先保存在：[浏览器本地/云端]；AI 只用于：[具体按钮]。
请先不要写代码。先输出：
1. 一页产品蓝图；
2. 页面与数据结构；
3. 每个 AI 按钮的动态输入、输出格式、模型级别和 token 上限；
4. 分成 3–5 个可验证开发阶段；
5. 每阶段验收标准和风险。
等我确认后，再只实现第一阶段。
```
